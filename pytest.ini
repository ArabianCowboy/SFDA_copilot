[pytest]
addopts = -ra
base_url = http://127.0.0.1:5001
markers =
    browser: browser-based Playwright tests
    integration: tests requiring processed search artifacts or external services
testpaths = web/tests
