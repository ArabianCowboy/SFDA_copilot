{
  "//": "No bundler and no node_modules: these packages are loaded in the browser from cdn.jsdelivr.net, so this file exists only so `npm audit` covers what users actually run. Versions are pinned exactly and MUST be kept in sync with the URLs in static/js/modules/ui.js, static/js/modules/stream-render.js, static/js/modules/services.js and web/templates/index.html.",
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "2.39.7",
    "bootstrap": "5.3.0",
    "dompurify": "3.4.13",
    "marked": "12.0.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.9.6"
  },
  "scripts": {
    "lint": "eslint static/js",
    "lint:fix": "eslint static/js --fix",
    "format": "prettier --write \"static/js/**/*.js\"",
    "format:check": "prettier --check \"static/js/**/*.js\"",
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "format:md": "prettier --write \"**/*.md\""
  }
}
