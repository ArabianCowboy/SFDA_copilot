{
  "//": "No bundler and no node_modules: these packages are loaded in the browser from cdn.jsdelivr.net, so this file exists only so `npm audit` covers what users actually run. Versions are pinned exactly and MUST be kept in sync with the URLs in static/js/modules/ui.js, static/js/modules/stream-render.js, static/js/modules/services.js and web/templates/index.html.",
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "2.74.0",
    "bootstrap": "5.3.0",
    "dompurify": "3.4.13",
    "marked": "12.0.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.9.6"
  },
  "//overrides": "markdownlint-cli2@0.23.2 pins smol-toml to exactly 1.7.0, which carries GHSA-7w5x-hrqm-74c2 (a denial of service on malformed TOML). 1.8.0 is the fix and is API-compatible; the only alternative npm offers is downgrading the linter itself. Nothing here reaches a browser or the server — smol-toml parses a markdownlint TOML config, and this repo's config is .markdownlint-cli2.yaml — so this closes the advisory, not an exposure. Drop the override once markdownlint-cli2 ships a release pinning 1.8.0 or later.",
  "overrides": {
    "smol-toml": "1.8.0"
  },
  "scripts": {
    "lint": "eslint static/js",
    "lint:fix": "eslint static/js --fix",
    "format": "prettier --write \"static/js/**/*.js\"",
    "format:check": "prettier --check \"static/js/**/*.js\"",
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "format:md": "prettier --write \"**/*.md\""
  }
}
